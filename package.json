{
  "name": "resume-ai-pro",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n \"FRONTEND,BACKEND,DB_SEED\" -c \"cyan,magenta,green\" \"npm run dev --workspace=resume-ai-frontend\" \"npm run dev --workspace=resume-ai-backend\" \"node seed.js\"",
    "build": "npm run build --workspaces --if-present",
    "start": "npm run start --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prisma": "^5.14.0"
  },
  "dependencies": {
    "@nestjs/platform-express": "^11.1.27",
    "firebase": "^12.15.0"
  }
}
